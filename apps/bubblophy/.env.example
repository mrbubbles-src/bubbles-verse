# Canonical origin only; MCP resource and audience become <origin>/mcp.
# NEXT_PUBLIC_APP_URL=http://bubblophy.mrbubbles.test:3005
# NEXT_PUBLIC_AUTH_COOKIE_DOMAIN=.mrbubbles.test
# Must belong to the same environment as DATABASE_URL and the OAuth Server.
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only; never expose this value through NEXT_PUBLIC_*.
# DATABASE_URL=
