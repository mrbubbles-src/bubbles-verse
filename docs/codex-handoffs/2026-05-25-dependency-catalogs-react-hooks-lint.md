# Codex Handoff: Dependency Catalogs And React Hooks Lint

Date: 2026-05-25
Repo: `/Users/mrbubbles/dev/bubbles-verse`
Branch: `new-app/dashboard`
Commit: `a5576a9`
Working tree: clean before this handoff file was created

## Current Goal

Continue from the completed dependency/catalog upgrade work and, if desired,
handle the remaining React Hooks lint warnings carefully. The user suspects some
warnings may be false positives and wants documentation-grounded judgment before
changing behavior.

## Completed

- Ran the Next/React upgrade flow earlier in the session and committed the
  resulting catalog/dependency work.
- Added Bun catalogs for shared dependency groups beyond Next/React, including
  tooling, testing, database, icons, Editor.js, Markdown, forms-validation,
  UI runtime, Supabase, Cloudinary, email, PDF, Node types, and app runtime.
- Updated `@bubbles/ui` from Zod 3 to Zod 4 through the `forms-validation`
  catalog.
- Updated `shiki` and `@shikijs/transformers` to `4.0.2`.
- Kept `pdfjs-dist` pinned to `5.4.296` because `react-pdf@10.4.1` depends on
  exactly that version; updating the app-level worker could create a PDF.js
  worker/API mismatch.
- Confirmed ESLint 10 is currently blocked by Bun `minimum-release-age`; leave
  it for later.
- Committed the dependency/catalog changes:
  `a5576a9 chore: centralize workspace dependency catalogs`.
- Checked current React lint warning docs via Context7/React docs and concluded
  several warnings are compiler-oriented diagnostics, not guaranteed bugs.

## Files Touched Or Investigated

Touched:

- `package.json` - added/expanded Bun catalogs and updated shared versions.
- `bun.lock` - regenerated by `bun install` after dependency/catalog updates.
- `README.md` - documented Bun `1.3.14` metadata and catalog groups.
- `CHANGELOG.md` - documented cross-cutting catalog/dependency updates.
- `apps/dashboard/package.json` - moved shared deps to catalogs and updated Bun
  package manager metadata.
- `apps/it-counts/package.json` - moved shared deps to catalogs.
- `apps/portfolio/package.json` - moved shared deps to catalogs, including PDF,
  email, forms, UI runtime, and app-runtime groups.
- `apps/teacherbuddy/package.json` - moved shared deps to catalogs.
- `apps/the-coding-vault/package.json` - moved shared deps to catalogs.
- `packages/eslint-config/package.json` - moved tooling deps to catalogs.
- `packages/markdown-editor/package.json` - moved Markdown/tooling deps to
  catalogs.
- `packages/markdown-renderer/package.json` - moved Markdown/tooling deps to
  catalogs.
- `packages/theme/package.json` - moved shared deps to catalogs.
- `packages/ui/package.json` - moved UI/form/runtime deps to catalogs and
  updated Zod through `catalog:forms-validation`.

Investigated:

- `AGENTS.md` - repo instructions, including Bun-from-root, docs/changelog, and
  verification expectations.
- `packages/ui/src/hooks/use-mobile.ts` - `set-state-in-effect` warning; likely
  small fix possible but not a bug.
- `apps/the-coding-vault/hooks/use-mobile.ts` - same mobile hook warning.
- `packages/theme/src/theme-provider.tsx` - hydration flag warning; likely an
  intentional client-mounted-state pattern.
- `packages/markdown-editor/src/components/markdown-editor.tsx` - draft/editor
  synchronization warnings; avoid blind refactor.
- `packages/ui/src/components/bubbles-sidebar/bubbles-sidebar-nav.tsx` - route
  active state opens sidebar group; warning needs behavior-preserving care.
- `apps/teacherbuddy/hooks/use-student-generator.ts` - Local Storage hydration
  warning; likely intentional external browser state.
- `apps/dashboard/app/login/page.tsx` - URL-derived login error warning; can be
  refactored but not proven broken.
- `apps/the-coding-vault/components/layout/auth/login-form.tsx` -
  `window.location.href` immutability warning; `window.location.assign(...)` is
  the smallest equivalent cleanup.
- React docs for `eslint-plugin-react-hooks`, `set-state-in-effect`, and
  `immutability` via Context7.

## Commands And Checks Run

- `git rev-parse --show-toplevel` - repo root confirmed.
- `git branch --show-current` - branch is `new-app/dashboard`.
- `git rev-parse --short HEAD` - current commit is `a5576a9`.
- `git status --short` - clean before handoff file creation.
- `git show --stat --oneline --decorate HEAD` - confirmed dependency/catalog
  commit contents.
- `bun install` - passed after final dependency changes.
- `bun run format` - passed.
- `bun run lint` - passed with warnings only.
- `bun run typecheck` - passed.
- `bun run test` - passed.
- `ncu --packageManager bun --workspaces --root --target minor --cooldown 3` -
  showed only `pdfjs-dist` and `prettier-plugin-tailwindcss` minor candidates;
  `pdfjs-dist` intentionally pinned.
- `ncu --packageManager bun --workspaces --root --target latest --cooldown 3` -
  showed later/major candidates including TypeScript 6, Vite React plugin 6,
  jsdom 29, Nodemailer 8, ESLint/@eslint-js 10, and PDF.js.
- `npm view react-pdf@10.4.1 dependencies peerDependencies version --json` -
  confirmed exact `pdfjs-dist: 5.4.296`.
- `npm view @eslint/js@10.0.1 peerDependencies dependencies version --json` -
  confirmed `@eslint/js@10` peers on `eslint@^10.0.0`.
- `bun install` with ESLint 10 trial - failed because Bun
  `minimum-release-age` blocked `eslint@^10.4.0`; changes were reverted.
- `git commit -m "chore: centralize workspace dependency catalogs"` - created
  commit `a5576a9`.

## Known Errors, Warnings, Or Failing Checks

- `bun run lint` passes, but React Hooks warnings remain:
  - `react-hooks/set-state-in-effect` in mobile hooks, theme hydration,
    markdown editor draft/editor sync, dashboard login page, teacherbuddy Local
    Storage hydration, and sidebar active-group opening.
  - `react-hooks/immutability` for
    `apps/the-coding-vault/components/layout/auth/login-form.tsx` assigning
    `window.location.href`.
- React docs indicate `set-state-in-effect` mainly warns about extra render
  passes and derived state that should be calculated during render. It does not
  mean every `setState` in an effect is a bug.
- Some current warnings are likely intentional browser/external-state sync
  patterns. Treat them as diagnostics to evaluate, not automatic refactor
  instructions.
- `ncu` reports `pdfjs-dist 5.4.296 -> 5.7.284`, but this is intentionally
  pinned to match `react-pdf@10.4.1`.
- `ncu` reports `prettier-plugin-tailwindcss ^0.7.4 -> ^0.8.0`; this was left
  for a later deliberate upgrade.
- `ncu` latest reports ESLint 10, but Bun currently blocks install by
  `minimum-release-age`.

## Open Decisions

- Decide whether to commit this handoff file or leave it local.
- Decide whether to fix only trivial lint warnings:
  `window.location.href` -> `window.location.assign(...)` and possibly both
  `use-mobile` hooks.
- Decide whether to leave intentional hydration/external-state warnings as-is,
  add narrow inline suppressions with explanatory comments, or tune shared ESLint
  config.
- Decide when to handle the "bewusst später" upgrade set:
  TypeScript 6, `@vitejs/plugin-react` 6, `jsdom` 29,
  `prettier-plugin-tailwindcss` 0.8, and Nodemailer 8.
- Decide when to revisit ESLint 10 after Bun `minimum-release-age` allows it.

## Constraints, Preferences, And Do-Not-Touch Areas

- Follow `AGENTS.md`.
- User prefers concise, simple, maintainable work.
- Ask before significantly changing a file.
- Always run formatting, linting, and typechecking before finishing code changes.
- For dependency changes, run `bun install` and `bun add` only from the repo
  root.
- Do not blindly refactor React Hooks warnings; verify against React docs and
  preserve behavior.
- Do not update `pdfjs-dist` beyond `5.4.296` while `react-pdf@10.4.1` depends
  on that exact version.
- Do not touch shadcn component files directly without a concrete reason.
- Do not revert user changes.

## Next Steps

1. Run `git status --short` and note this handoff file is likely untracked.
2. If continuing lint cleanup, start with the smallest safe change:
   `window.location.href = res.url` to `window.location.assign(res.url)`.
3. Re-check `bun run lint` after that single change and confirm the
   `immutability` warning disappears.
4. Evaluate both `use-mobile` hooks next; prefer a behavior-preserving
   `useSyncExternalStore` or guarded lazy-init approach over ad hoc state moves.
5. Leave markdown editor, teacherbuddy hydration, theme hydration, dashboard
   login feedback, and sidebar route-open behavior alone unless explicitly
   approved.
6. For any functional lint cleanup, run at least `bun run format`,
   `bun run lint`, `bun run typecheck`, and relevant tests.

## Reactivation Prompt

```text
Continue this work from the handoff document:
/Users/mrbubbles/dev/bubbles-verse/docs/codex-handoffs/2026-05-25-dependency-catalogs-react-hooks-lint.md

Work in repo:
/Users/mrbubbles/dev/bubbles-verse

Start by reading the handoff and any repo instructions such as AGENTS.md. Verify the current branch and working tree with git status before editing. Do not rely on old chat context; treat the handoff and repository as the source of truth.

Current goal:
Continue from the completed dependency/catalog upgrade and evaluate React Hooks lint warnings carefully, using docs-backed judgment and minimal behavior-preserving fixes only where worthwhile.

Important constraints:
- Follow AGENTS.md.
- Keep communication concise.
- Run Bun commands from the repo root.
- Do not blindly refactor React Hooks warnings; several may be intentional browser/external-state sync.
- Keep pdfjs-dist pinned to 5.4.296 while react-pdf is 10.4.1.
- Do not revert user changes.

Begin with the next steps listed in the handoff, and report any mismatch between the handoff and the current repo state before changing files.
```
